const fs = require('fs');
let code = fs.readFileSync('server.ts', 'utf8');

// I will just rewrite generateHighQualityBase64 and the start of vertex-generate-image
// I need to see what I broke first
