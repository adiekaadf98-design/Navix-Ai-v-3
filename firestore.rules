rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // [DEVELOPMENT / TRUE BYPASS] 
    // Allowing all read/writes globally so the local stealth bypass works flawlessly
    // without requiring Firebase Authentication to be fully configured.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
